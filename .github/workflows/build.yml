name: Build Launcher-NX

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    container: devkitpro/devkita64:latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      # 1. Clone and compile the entire libnx system top-to-bottom to guarantee crt0.o is built
      # Fixed: Running top-level make instead of just the /nx subdirectory
      - name: Compile Native libnx Framework
        run: |
          export PATH=$DEVKITPRO/devkitA64/bin:$PATH
          
          # Force join separate string segments to completely bypass character filtering bugs
          TARGET_URL=https://github.com/switchbrew/libnx.git
          git clone --depth 1 "$TARGET_URL" libnx_src
          
          # Build the entire framework repository top-to-bottom so crt0.o and spec sheets generate
          make -C libnx_src -j$(nproc)
          
          # Map structural asset dependencies out into the temporary workspace folder
          mkdir -p ./sys_deps/include ./sys_deps/lib
          cp -r libnx_src/nx/include/* ./sys_deps/include/
          
          # Relocate the freshly compiled binary files
          find libnx_src -name "libnx.a" -exec cp {} ./sys_deps/lib/ \;
          find libnx_src -name "crt0.o" -exec cp {} ./sys_deps/lib/ \;
          find libnx_src -name "switch.specs" -exec cp {} ./sys_deps/lib/ \;

      # 2. Compile against our newly built, local library files
      - name: Compile and Link Launcher-NX
        run: |
          export PATH=$DEVKITPRO/devkitA64/bin:$PATH
          
          # Compile main.cpp using our local system search headers
          aarch64-none-elf-g++ -O2 -ffunction-sections -fdata-sections -fPIE -march=armv8-a+crc+crypto -mtune=cortex-a57 -I./sys_deps/include -c source/main.cpp -o main.o
          
          # Link explicitly against the newly compiled framework library to fix all relocation errors
          aarch64-none-elf-g++ -specs=./sys_deps/lib/switch.specs -march=armv8-a+crc+crypto -mtune=cortex-a57 -Wl,-Map,Launcher-NX.map -fPIE -pie main.o ./sys_deps/lib/crt0.o -L./sys_deps/lib -lnx -o Launcher-NX.elf
          
          # Package the tailored binary directly into your clean executable Switch .nro format
          elf2nro Launcher-NX.elf Launcher-NX.nro --title="Launcher-NX" --author="Homebrew Dev" --version="1.0.0"

      # 3. Deliver your completed, stable Launcher-NX file bundle
      - name: Upload Launcher-NX
        uses: actions/upload-artifact@v4
        with:
          name: Launcher-NX
          path: ./Launcher-NX.nro
